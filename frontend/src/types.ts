export interface NavigationItem {
  icon: string;
  label: string;
  isActive?: boolean;
}

export interface StudyCard {
  title: string;
  description: string;
}

export interface ResourceImage {
  src: string;
  alt: string;
}
